# Hall C optics handoff — 15 September 2026

Branch: `core-sample-campaign`. Test campaign: `HMS_6p667GeV`.
Continue the existing analysis without rebuilding completed products gratuitously.

## Current products and conclusions

- Frozen balanced sample: `equal15`; 68,535 training core events, 241,978 surplus cores.
- Protected comparison: 77,864 cores, 22,532 noncore/shoulder events, 638 unsupported-region events; blocked positions counted separately.
- Beam result: `06e_beam_search/beam10`, width 10, up to 40 steps. Direct scaled-X SVD refits; constants fitted; xtar-dependent terms and delta coefficients inherited/fixed.
- Five-model comparison is exposed under `07_diagnostics/compare`: old replay, GMM, full-core SVD, elastic-net-selected unpenalized SVD, beam. PNGs, TSVs and manifest are local/GitHub; full event residual NPZ remains on ifarm.
- Beam versus compact seed improves protected-core xptar RMS 1.5538→1.4956 mrad, improving all 25 foil/delta cells. Full-core SVD is still slightly better globally (1.4886 mrad). Beam mainly preserves predictive accuracy with reduced basis and better conditioning; do not claim broad superiority over full-core SVD.
- Beam versus GMM improves low-delta protected-core xptar RMS about 6–14% across five foils. Most gain occurred at the full-core fit stage; selection, balancing and solver effects were not isolated. No clear pooled shoulder/noncore improvement over GMM.
- Legacy offset application remains flagged by the user as wonky. Do not treat historical bias/RMS comparisons as settled physical calibration. Per-foil widths and complete metrics are in the comparison TSVs; RMS is not a Gaussian resolution.

## Small unfinished tasks

1. Comparison plot revisions are implemented: events/bin (%), linear horizontal tail view with percentage labels and 10%/1%/0.1% guides, separate full-range extremes, and percent-change heat maps centered on zero. Refresh on ifarm with `./run_compare.sh HMS_6p667GeV equal15 compare --replot`. This uses saved `compare/residuals.npz` and `tsv/residuals.tsv`; it does not evaluate matrices or repeat statistics. Figures and their checksums are replaced, plotting code archived, numerical results unchanged. Read `MATRIX_COMPARISON.md` and the generated `plots/README.md`.
2. A fresh `run_compare.sh` evaluation still has the earlier statistics bottleneck. The inspected problem includes Python `sum(sel)` over the full event mask inside the 4-target × 5-model loop for every group in `comparison_plots.py`. Avoid repeated scans/counting and reuse group indices; add progress in a future targeted fix. Use `--replot` for the current plot refresh instead of rerunning the slow job.
3. Standalone sieve afterburner IS implemented and pushed (code commit b6b1fcf). Synthetic math, selection and six-PNG integration checks passed. Actual ifarm closure/results are not yet available in this workspace; ask whether the user already ran it before instructing duplicate runs.

From the repository root on ifarm:

```bash
git pull --ff-only
./run_sieve.sh HMS_6p667GeV --check
./run_sieve.sh HMS_6p667GeV
```

This reads campaign config paths for rg01/run 1544 (foil 0) and rg03/run 1540 (foils ±8), exact angle 12.490°. Cuts: original replay delta strictly −10 to +10%, Cherenkov >2, calorimeter E/p >0.65, original reaction-z within ±2 cm of foil. No core/GMM event cuts. Two matrices × three foils = six plots with common axes/binning/color scale. Output: `07_diagnostics/sieve/`.

It keeps saved tracks/reaction vertices and repeats the HCANA extended-target iteration. It is NOT a complete new reaction-vertex solve or replay. Closure uses oldfit.dat and zero external offsets, appropriate to the configured zero-offset source replay. Warnings are retained on plots, not suppressed. Read `SIEVE_AFTERBURNER.md`. Expose `plots/*.png`, `tsv/*.tsv`, `manifest.json`; retain `histograms.npz` for inexpensive replotting. Existing output directories cannot be overwritten.

## Substantive work for the next conversation

- Read `HMS_GEOMETRY_CONSISTENCY.md`: standalone informal proof with definitions and pinned web/code references. Two target-equation projection mismatches are algebraically identified; their physical interpretation and historical replay conventions still require checking. No geometry code was changed. Do not turn a conditional proof into a claim of measured error or blindly patch target equations.
- User is interested in weighted SVD using all development cores (training plus surplus), preserving the intended population weights. Keep protected events out of training and isolate this change from basis/geometry changes.
- Explain Huber loss before proposing its separate trial; do not confuse robust residual weighting with population balancing.
- Examine systematic hole-centroid displacement versus within-hole width, using existing per-hole statistics and avoiding noisy sparse-hole conclusions.
- Changes to xtar-dependent coefficients are deferred by the user.
- Elastic/delta scans follow a stable angular matrix; do not expand into that task now.
- Extra listed 6.667 runs 1541–1543 were short false starts per user, not useful new statistics. Do not recommend them again based on the wiki alone.
- Nearby 6.59 data are a possible later opportunity but must not be pooled under an assumed identical transport matrix. User explicitly emphasized momentum-setting dependence. Wiki run 4819 lists −10, 0, +10 cm foils and different era/polarity conventions.

## Working preferences

Be concise, use clear physics language, and focus on high-payoff tests. No broad redesign, long beam searches or repeated exhaustive checks. Make small tasks reproducible, preserve existing outputs, and distinguish internal validation from independently established reconstruction performance. No further data can be collected at this setting. The user runs commands on ifarm; this workspace is on the Mac.
